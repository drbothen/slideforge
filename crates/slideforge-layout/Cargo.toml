[package]
name = "slideforge-layout"
description = "Core layout engine — transforms a semantic Deck into a geometric LaidOutDeck with EMU coordinates"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "../../README.md"

[lib]
name = "slideforge_layout"

[dependencies]
# All production deps pinned with `=` per supply-chain policy.
slideforge-types = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
insta = { version = "=1.47.2", features = ["yaml"] }
proptest = "=1.5.0"
tracing-test = "=0.2.5"
tracing-subscriber = { workspace = true }
